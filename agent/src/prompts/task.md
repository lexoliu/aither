Launch a specialized subagent to handle complex tasks autonomously.

Use this for multi-step tasks that require autonomous exploration, research, or analysis.
Subagents run independently and return their findings.

Available subagent types:
{{types}}
